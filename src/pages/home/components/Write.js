import React, { PureComponent } from 'react';
import { WriterWrapper } from '../style';

class Write extends PureComponent {
	render () {
		return (

			<WriterWrapper>Notes</WriterWrapper>
			)
	}
}

export default Write;